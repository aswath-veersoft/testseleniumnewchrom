language: java

jdk:
  - oraclejdk8

build:
  ci:
    - nohup sudo Xvfb :10 -ac
    - export DISPLAY=:10
    - mvn appengine:test

integrations:
  notifications:
    - integrationName: email
      type: email
      on_success: change
      on_failure: always
      on_pull_request: never
